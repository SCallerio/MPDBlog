# Python dependencies for the MPD Blog Jekyll site's data/notebook pipeline
# (BSEE well scraping, drilling-contractor scraping, dashboard notebooks,
# and the build-notebook.ps1 -> nbconvert -> Jekyll include workflow).
#
# Setup:
#   python -m venv C:\venvs\MPD_Blog
#   C:\venvs\MPD_Blog\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# Ruby/Jekyll dependencies are tracked separately in Gemfile / Gemfile.lock.

# --- Data wrangling ---
pandas==2.3.3
numpy==2.4.0

# --- Web scraping / HTTP (drilling_contractor_scrap.ipynb, script/process_bsee_wells.py) ---
requests==2.32.5
beautifulsoup4==4.14.3

# --- Plotting (dashboard + rig/operator summary notebooks) ---
matplotlib>=3.9
plotly==6.5.0
kaleido>=0.2.1

# --- Interactive widgets (connections dashboard notebook) ---
ipywidgets>=8.1

# --- Jupyter / notebook build pipeline (build-notebook.ps1) ---
jupyterlab==4.5.1
notebook==7.5.1
nbconvert==7.16.6
nbformat==5.10.4
ipykernel==7.1.0
